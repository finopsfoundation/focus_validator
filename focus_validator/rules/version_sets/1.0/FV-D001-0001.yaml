dimension: ChargeType
data_type: string
check_friendly_name: "ChargeType must have a value from the list: {values}."
validation_config:
  check:
    value_in:
      - "Adjustment"
      - "Purchase"
      - "Tax"
      - "Usage"
